server:
  port: 7000
spring:
  application:
    name: customer-service
  datasource:
    url: jdbc:postgresql://customer-db:5432/customerDb
    username: postgres
    password: 54321
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      jakarta:
        persistence:
          validation:
            mode: none
    show-sql: true